version: "3.9"
services:
  mcp-coinbase:
    build: .
    container_name: mcp-coinbase
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8001:8001"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
